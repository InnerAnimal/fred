# Core
NODE_ENV=production
APP_URL=https://YOUR_DOMAIN_OR_VERCEL_URL

# Supabase (public anon only in frontend, service key stays server-side)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=   # server-only

# Stripe (publishable in frontend, secret only server-side)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=

# Google SMTP Relay (server-side)
SMTP_HOST=smtp-relay.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Meauxbility <noreply@meauxbility.org>"

# Logging/Analytics (optional)
POSTHOG_KEY=
SENTRY_DSN=

# GitHub (optional for automations)
GITHUB_OWNER=InnerAnimal
GITHUB_REPO=fred
GITHUB_TOKEN=
